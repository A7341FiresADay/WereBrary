using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class bookshelf : MonoBehaviour {

	public GameObject book;


	// Use this for initialization
	void Start () {
	}



	void update(){
		/*foreach(var book in books){
			if(book.bookIndicator == null){
				book.bookIndicator = new TextMesh();//Instantiate(TextMesh, transform.position, Quaternion.identity);
				book.bookIndicator.transform.parent = transform;
				book.bookIndicator.transform.position = Vector3(0, 0, 0);
				(TextMesh)book.bookIndicator.text = "heyhey";
			}
			//book.bookIndicator.transform.rotation = 
		}*/
	}


	
	

}